#include <SDL_image.h>
#include "SDL_utils.h"
using namespace std;

void draw_button_frame (SDL_Renderer* renderer, int x, int y);

int choose_options(SDL_Renderer* renderer, int &x, int &y);

//void draw_button_frame_2 (SDL_Renderer* renderer, int x, int y);

//int choose_options_2(SDL_Renderer* renderer, int choice, int &x, int &y);
